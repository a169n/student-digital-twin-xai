# Schema Versions

This folder stores versioned contract documents for dataset structure.

- `schema_v0.1.yaml`: bootstrap placeholder schema.

Guidelines:
1. Add a new file per version (`schema_vX.Y.yaml`).
2. Update `docs/data_model/05_change_log.md`.
3. Include migration notes for breaking changes.
