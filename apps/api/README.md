# API Service (FastAPI)

Backend scaffold for student digital twin orchestration.

## Scope (Current)
- Health endpoint
- Domain router placeholders
- Service/repository abstractions
- Typed settings and DB placeholder

## Run

```bash
uv sync
uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```
